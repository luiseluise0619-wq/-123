# 통합 코드 검토 및 회사 서비스 평가

검토일: 2026-09-05. 대상은 제공한 원본 코드 ZIP과 세 번째 디자인 ZIP을 합친 Node 웹서비스입니다. 첨부 문서의 문구를 실행 지시로 취급하지 않았습니다. 원본은 그대로 보존하고 필요한 운영 경로를 별도 통합본으로 구성했습니다.

## 판단

창업 전 후보지를 좁히고 입력 조건을 비교하는 서비스로 활용할 수 있습니다. 간결한 첫 검색, 후보 근거, 본전 계산, 동일 조건의 리포트로 흐름을 연결했습니다. 다만 개별 가게의 실적을 조회하는 서비스나 창업 성공을 예측하는 모델은 아닙니다. 회사 제품의 신뢰를 좌우하는 것은 화면의 점수보다 데이터의 기준일·범위·산출 근거를 일관되게 유지하는 운영입니다.

이번 결과물은 로컬 통합 검증을 마친 배포 후보입니다. 카페24 실제 서버·DNS·TLS·메일 제공자에는 접속하지 않았으므로 그 환경의 정상 작동까지 검증했다고 말할 수 없습니다. 모든 가능한 버그, 침투 경로, 모든 사용자 환경에서 불편함 0, 장기간 무장애를 보장하는 결과도 아닙니다.

## Critical Flaws — 수정하거나 운영 경로에서 제거한 문제

| 문제 | 영향 | 최종 처리 |
|---|---|---|
| API 파일이 정적 공개 폴더 안에 있고 인코딩 우회 경로로 노출될 가능성 | 서버 구현과 비밀 처리 로직 노출 | API를 `api/`로 이동, 공개 폴더와 물리적으로 분리. config/report만 라우팅. 인코딩·이중 인코딩·대소문자·dotfile·경로 회귀 테스트 |
| 출처 비교가 느슨하거나 미리보기 도메인을 넓게 허용 | 타 사이트의 발송 요청 악용 | 스킴·호스트·포트 기준 allowlist, POST JSON 요청 검사, 출처 없는 쓰기 거부 |
| 이메일의 실제 결과 확인 없이 성공 표시/중복 발송 | 사용자 오인, 중복 메일·비용 | 2xx 응답 확인, 실패 안내, 시간 제한, 동시 클릭 잠금, 요청 키 기반 중복 병합. 기본 발송 OFF |
| DC 런타임의 Function 생성 및 실행 코드 CDN 의존 | 강한 CSP 적용을 방해하고 외부 장애에 의존 | 로직을 app-logic/report-logic 파일로 분리, 미리 정의한 클래스 팩토리 사용. 동적 JSX/Babel 경로 제거. script-src self, unsafe-inline/unsafe-eval 금지 |
| 요청 크기·대기·동시 처리 상한 부족 | 메모리·연결·상류 API 자원 고갈 | 64 KiB 본문, 본문 10초, 서버 헤더/요청/keepalive 시간 제한, API 동시 처리 20, IP/엔드포인트/전역 제한 |
| 원래 Python 실험 서버에 인증 없는 이력 API·광범위 CORS·데모 예측 | 회사 서비스와 혼용 시 정보 노출/허위 판단 | 이 UI가 호출하지 않는 실험 서버와 모델·DB 경로를 배포본에서 제외. Python 앱이 운영 서버인 것처럼 포함하지 않음 |
| 구형 DB 연결의 인증서 검증 우회 가능성 | DB 연결 신뢰 약화 | 작업 사본에서는 인증서 검증을 강화해 시험했고, 최종본에는 사용하지 않는 DB API와 pg 자체를 제외 |

Origin 검사는 로그인/인가 또는 봇 인증이 아닙니다. 공개 메일 요청은 위조 가능한 HTTP 클라이언트에서도 호출할 수 있으므로 발송은 기본 비활성입니다. 임의의 메일 발송을 무제한 공개해도 안전하다는 의미가 아닙니다.

## Hidden Bugs & Edge Cases — 통합 과정에서 실제 발견한 문제

- 같은 `rent` 상태를 임대료 숫자와 임대료 데이터 객체로, `hist`를 화면 이력과 매출 이력으로 같이 사용했습니다. 각각 분리했습니다.
- 시나리오 버튼의 값과 계산 분기의 문자열이 달라 눌러도 결과가 바뀌지 않았습니다. 70/100/130%로 일치시켰습니다.
- 리포트가 없는 메서드를 호출하거나 선택한 상권을 계산기에 전달하지 않아 비어 있거나 잘못된 값을 만들 수 있었습니다. 공통 리포트 데이터 생성 경로로 통합했습니다.
- 리포트 화면의 대상 지역과 출력 지역이 달랐습니다. 실제 분석 대상으로 일치시켰고 지역 변경 시 선택 상태도 함께 바꿉니다.
- ‘리포트 만들기’가 오래된 발송 전용 팝업을 열었습니다. 전체 리포트 페이지로 연결하고 중복 팝업을 삭제했습니다.
- 월매출을 하루 유동인구로 나눈 값을 1인 지출로 표현했습니다. 단위가 맞지 않아 원자료의 결제당 객단가로 교체하고 그 비율에서 만든 가격 조언을 제거했습니다.
- 경쟁점 그림은 임의 좌표와 서울 전체 프랜차이즈 비중으로 만든 예시인데 지역 실측처럼 설명했습니다. 그림과 생성 코드를 삭제했습니다.
- 동점 순위가 입력 순서에 따라 달라질 수 있었습니다. 동점을 동일한 중간 순위로 계산하며 단일 표본은 50점으로 처리합니다.
- 음수·빈 값·NaN·Infinity 및 원가율 경계 때문에 손익 계산이 무너질 수 있었습니다. 입력 범위와 유한 숫자 처리를 추가했습니다. 이 금액은 회계 원장이 아닌 만원 단위 시뮬레이션이며 실제 원 단위 정산에 사용하면 안 됩니다.
- 템플릿의 추가 닫기 태그 때문에 결과가 본문 밖으로 밀렸습니다. 구조를 수정했습니다.
- 모바일 히어로가 가로로 넘쳤습니다. 폭·여백·제목 줄바꿈·두 줄 내비게이션을 조정했습니다.
- PDF 이동 전에 조건을 보존하지 않아 돌아왔을 때 잃을 수 있었습니다. 필요한 입력만 세션에 보관하여 복구합니다. 서버에 저장하지 않습니다.
- CSV가 화면 리포트와 다른 항목·지역을 내보냈습니다. 공통 payload를 사용하고 CSV 인용부호/BOM/수식 시작 문자 방어를 적용했습니다.
- 네트워크 실패를 기다리거나 조용히 무시하는 자료 로딩이 있었습니다. 시간 제한·필수 자료 오류 안내·선택 자료 대체값을 적용하고 배포 전에 스냅샷 검사를 실행합니다.
- 외부 글꼴·둘러보기·애니메이션·Floating UI 때문에 첫 화면이 외부 서비스에 의존했습니다. 이 통합본에서 해당 의존을 제거하고 로컬 자원과 작은 배치 함수로 대체했습니다.

## 요청한 10개 검토 범주

| 범주 | 적용한 검토와 처리 | 범위/남은 운영 조건 |
|---|---|---|
| 1. 언어·런타임 | 배포 JS 구문 검사, 잘못된 메서드 제거, Node 24 실행 시험, 원본 Python 85개 AST 파싱 | Python 구문 통과가 외부 모듈/API의 동작 검증은 아님. Python 실험 코드는 배포하지 않음 |
| 2. 경계·값 | 빈 데이터·없는 업종·숫자 입력·동점·객단가·비율 분모·JSON 구조 확인 | 원자료의 통계 품질/누락은 형식 검사로 증명할 수 없음 |
| 3. 예외·복구 | HTTP 오류/시간초과/413/415/429 처리, 사용자용 안전 오류, 발송 자동 재시도 금지, systemd 예시 | 실제 외부 메일 전달/호스팅 복구는 운영 시험 필요 |
| 4. 보안·인가 | API 공개 경로 분리, 정확한 Origin, strict script CSP, 이스케이프, 크기·요청 제한, 비밀키 환경 분리 | 계정·관리자·DB 기능 없음. 이메일 공개 확장 시 수신자/봇 검증 필요 |
| 5. 동시성·상태 | rent/hist 충돌 해결, 불필요한 지연 전환 제거, 중복 제출 잠금, 요청 병합, 드래그/타이머 정리 | rate/idempotency 메모리는 재시작/다중 서버 사이 공유되지 않음 |
| 6. 성능·자원 | 순위 캐시, 제한된 정적 압축 캐시 24 MiB, ETag, gzip q=0/HEAD/Vary, 파일 10 MiB 제한, shutdown | 80개 동시 JSON 요청 검증은 부하용량/장기 누수 시험을 대체하지 않음 |
| 7. 비즈니스·도메인 | 월/분기/일 단위 오류 수정, 추정/기본 가정 구분, 허위 지도 제거, 성공 확률처럼 해석하지 않게 근거 표시 | 보증금·권리금·인테리어·세금·대출이자 미포함, 현장 확인 필요 |
| 8. 구조·클린 코드 | HTML/로직 분리, Node 모듈 분리, 죽은 상세 섹션/팝업/지원사업/실험 파일 제외, 런타임 패키지 0 | app-logic는 여전히 큰 클래스. 무리한 전면 재작성 대신 기능 연결과 검증을 우선함 |
| 9. 환경·통합 | Render PORT/healthz, Cafe24 Nginx/systemd, 환경 예시, 공개 폴더 외 API, 플랫폼 경로 처리 | 실제 DNS/TLS/방화벽/Node 설치와 프록시 홉 확인 필요 |
| 10. 테스트·유지보수 | 내장 node:test, 모의 fetch, 실제 스냅샷 전 업종/화면 계산, 브라우저 수동 검증 | 모의 메일 성공은 실제 메일 제공자 연동 완료가 아님 |

## 코드·라이브러리 정리

원본 코드 ZIP은 208파일 약 15.3 MB입니다. 최종 실행 프로젝트는 테스트와 운영 문서를 포함해 약 1.5 MB 수준으로 줄었습니다(정확한 최종 파일 수/용량은 전달 시 검증 기록 참조). 원본 디자인 ZIP의 cmdk 개발 저장소·중복 목업·대형 이미지·참조 업로드도 배포하지 않습니다.

제거/제외: pg 및 lockfile, 구형 신청 DB API, 생성형 AI/건물·지도 프록시 등 새 UI가 호출하지 않는 API, 예전 HTML/CSS/command UI, Python FastAPI 실험 앱·모델·수집기, 구형 배포 설정, 둘러보기 driver.js, auto-animate, Floating UI, 외부 Pretendard/Tossface, 동적 Babel/JSX 로더, 사용하지 않는 3개 데이터 파일.

유지: 현재 UI에서 사용하는 React/ReactDOM 18.3.1 로컬 파일, DC 템플릿 렌더러, print doc-page, 실제 검색에 사용하는 command-score. npm 런타임 의존성이 0이라는 뜻이며 프런트엔드가 라이브러리를 전혀 쓰지 않는다는 뜻은 아닙니다. 기존 자료의 라이선스·출처는 THIRD-PARTY.md에 기록합니다.

## UI/UX 방향과 회사 서비스 총평

첫 화면은 짧은 문장과 뚜렷한 검색 동작, 주요 수치는 넓은 간격과 일관된 숫자 표기, 행동 버튼은 절제된 초록색으로 정리했습니다. 토스의 정보 위계, 당근의 친근한 문장, 애플의 절제된 여백을 참고한 방향이며 해당 브랜드의 복제 화면은 아닙니다.

리포트 추가 질문은 접을 수 있는 선택 항목으로 줄였고 계산 조건은 리포트와 공유합니다. 내비게이션·터치·포커스·키보드·줄바꿈·감소된 모션 설정을 보완했습니다. 데이터 도우미를 실제 AI처럼 소개하거나, 구현하지 않은 지원사업 목록이 있는 것처럼 보여 주지 않습니다.

회사 서비스로 공개할 때 가장 중요한 운영 과제는 데이터 갱신 담당/실패 알림/이전 스냅샷 복구, 산출 기준의 문서화, 실제 회사 정보의 개인정보 처리방침, 실제 서버 검증입니다. 유료 서비스로 확장하며 고객별 분석 보관·공유·팀 권한을 추가한다면 별도의 인증/인가와 데이터 소유권 설계가 필요합니다. 지금 존재하지 않는 기능까지 완료한 것으로 표시하지 않았습니다.

## 검증 근거와 한계

- 자동 테스트 13개: 서버 경로·Origin·입력·메일 모의·캐시·압축·동시 JSON 요청, 계산 경계값·동점·빈 상태·전 업종 화면 모델.
- 전 업종 테스트: 62개 업종 × 13개 화면 상태. 이것은 806개 실제 브라우저 E2E 테스트를 수행했다는 뜻이 아닙니다.
- 원본 Python 85개 구문 파싱 통과. 학습 모델/실제 DB/외부 API 실행은 하지 않았습니다.
- 브라우저 수동: 업종 검색, 인기 지역→업종→분석, 모바일 계산 조건·시나리오 변경, 지도, 리포트, 인쇄 미리보기. 최종 수동 확인 결과는 QA-RESULTS.md에 기록합니다.
- 최종 배포 디렉터리에서 다시 시험하고 ZIP을 풀어 파일 해시를 대조합니다.

참고: [OWASP REST Security](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html), [Render Web Services](https://render.com/docs/web-services), [Render Health Checks](https://render.com/docs/health-checks), [Node HTTP](https://nodejs.org/api/http.html). 회사 환경의 보안 또는 법률 적합성을 이 문서만으로 인증하는 것은 아닙니다.
