# MYSBIZON (사장님인사이트) — 작업 전에 읽는 문서

이 저장소에서 코드를 고치는 사람/도구가 **처음 5분 안에** 알아야 하는 것만 적었습니다.
프로젝트의 판단 기준·보안 규칙은 `CLAUDE.md` 에 있습니다. **둘 다 지켜야 합니다.**

---

## 1. 이게 무슨 앱인가

**서울 상권 1,564곳 × 업종 62가지**의 공개 데이터를 모아,
장사를 시작하려는 사장님이 **"어디서, 무엇을, 얼마에 하면 되나"** 를 판단하도록 돕는 웹서비스입니다.

만드는 사람은 개발 경험이 많지 않은 1인 개발자이고, **회사에서 실제로 쓸 서비스**입니다.
그래서 "화면이 그럴듯하게 나오는 것"보다 **숫자가 정직한 것**이 우선입니다.

### 가장 중요한 원칙 — 데이터 정직성

> **없는 값은 지어내지 않는다.**

- 값이 없으면 `available:false` / `configured:false` 로 두고 화면에 **"데이터 없음 / 아직 연결되지 않았어요"** 라고 씁니다.
- 실측 / 가정 / 추정 / 폴백을 **화면 문구와 코드 주석에서 구분**합니다.
  예) 환산임대료는 `보증금×12%÷12 + 월세` 로 계산한 **추정값** → 화면에 '추정'이라고 씁니다.
- 서울 평균값을 특정 상권 값인 것처럼 쓰지 않습니다 → `"서울 평균 · 이 상권 값은 아니에요"`.
- **자료가 없다는 이유로 지역·지표·기능을 UI 목록에서 지우지 않습니다.**
  지우면 "이 서비스는 환율을 안 다루는구나"가 됩니다. 남기면 "다루는데 아직 연결이 안 됐구나"가 됩니다. 둘은 다른 말입니다.
- 자격 판정(정부지원사업 등)은 **하지 않습니다.** "조건에 해당할 수 있는 공고"까지만 쓰고 원문 링크를 같이 줍니다.

---

## 2. 실행 · 검사

외부 npm 패키지가 **없습니다.** `npm install` 필요 없습니다. (Node 22~24)

```bash
npm start          # http://localhost:3000  (node server.js)
npm test           # 27개 — 반드시 통과해야 합니다
npm run check:data # frontend/data/v3/*.json 스키마 검사
npm run build:html # frontend/screens/*.html → frontend/index.html
npm run check:html # 커밋된 index.html 이 조각들과 같은지 (npm test 에 포함)
```

**`frontend/index.html` 을 직접 고치지 마세요.** 그건 빌드 결과물입니다.
`frontend/screens/*.html` 을 고치고 `npm run build:html` 을 돌립니다. 어긋나면 `npm test` 가 잡습니다.

---

## 3. 폴더 구조

```
frontend/          브라우저로 나가는 것 전부 (정적)
  index.html       ← 빌드 결과물. 직접 고치지 말 것
  screens/*.html   ← 화면 조각 (여기를 고친다)
  logic/*.js       ← 로직, 책임별로 분리
  app-logic.js     ← 상태·생애주기·renderVals()·조각 결합
  locales/*.json   ← ko / en / zh-CN 사전
  data/v3/*.json   ← 수집기가 만든 데이터 (앱이 읽는 것)
  vendor/          ← Chart.js 등. CSP 때문에 CDN 을 쓰지 않는다
  dc-runtime.js    ← 템플릿 프레임워크. 벤더 코드 — 고치지 않는다

api/               서버 API 핸들러 (공개 폴더 밖. 브라우저가 직접 못 읽는다)
server/            Node 표준 라이브러리만 쓴 정적+API 서버
backend/*.py       공공데이터 수집기 → frontend/data/v3/*.json 을 만든다
.github/workflows/ 수집기를 주기적으로 돌린다
tests/             node:test 27개
scripts/           build-html.mjs · validate-data.mjs
```

배포는 `node server.js` 한 덩어리입니다 (Render / Cafe24 VPS). `deploy/` 에 systemd·nginx 예시가 있습니다.
※ `CLAUDE.md §1` 에 "Vercel 정적 사이트"라고 적힌 부분은 **이전 단계 설명**입니다. 지금 실제 배포 형태는 위와 같습니다.

---

## 4. 메뉴 구조 — **빠짐없이 있어야 하는 것**

헤더 메뉴는 **4개**입니다. 각 메뉴는 **서로 겹치지 않는 질문 하나**에 답합니다.
정의 위치: `frontend/app-logic.js` 의 `buildVals()` 안 `const MENU=[...]` (319행 근처).
문구는 `frontend/locales/*.json` 에서 옵니다 — **여기에 한국어를 박아 넣지 마세요.**

| 메뉴 | 답하는 질문 | 안에 들어 있는 화면 (`screen` 키) |
| --- | --- | --- |
| **① 상권분석** `nav.zone` | **"어디가 좋지?"** — 여러 곳을 훑는다 | 허브 `hubZone` · 지역비교 `zone` · 후보지 `find` · 자치구 훑기 `fineCmp` · 지역 개요 `region` |
| **② 정밀분석** `nav.fine` | **"왜 좋은 거지? 어디로 정할까?"** — 고른 곳을 깊게 보고 정한다 | 허브 `hubFine` · 소개 `fineIntro` · 지도 `map` · **정밀분석 `fineDetail`** · **정밀비교 `sim`** · 본전 계산 `diag` |
| **③ 통합시세** `nav.market` | **"장사 환경은 어떤가?"** — 임대료·환율·원자재 같은 바깥 사정 | 통합시세 `price` |
| **④ 리포트** `nav.report` | **"어떤 지원을 받지?"** — 조건에 맞는 정부 창업지원사업 | 리포트 `report` |

### 절대 빠뜨리면 안 되는 것

- **① 상권분석과 ② 정밀분석은 둘 다 반드시 있어야 합니다.** 하나로 합치지 마세요.
  - 상권분석 = **여러 곳** 을 놓고 고르는 단계
  - 정밀분석 = **한 곳** 을 정한 뒤 파고드는 단계
  - 이 둘이 섞이면 "어디로 할까"와 "여기 왜 괜찮은가"가 한 화면에 뒤엉킵니다.
- **정밀비교(`sim`)는 ② 정밀분석 메뉴 안에** 있습니다. 최상위 메뉴로 빼지 마세요. (사장님이 명시적으로 그렇게 요청했습니다.)
- **정밀비교(`sim`)와 본전 계산(`diag`)은 하는 일이 다릅니다** — 이름이 비슷하니 헷갈리지 마세요.
  - `sim` 정밀비교 — 담아 둔 상권 3곳을 **공개 데이터**로 순위 매김 (종합순위 · 가중치 5가지). **내 숫자를 넣지 않습니다.**
  - `diag` 본전 계산 — **사장님이 직접 넣은 숫자**(평수·임대료·인건비…)로 한 자리의 본전선을 계산
- 2026-09-06 에 옛 `cmp` 비교분석 화면(① 상권분석 안)을 `sim` 자리로 옮겼고,
  내 숫자를 넣던 옛 정밀비교 계산기(`logic/sim.js`)는 지웠습니다.
  '내 숫자로 계산'하는 화면이 둘이라 무엇이 다른지 알 수 없다는 지적 때문입니다.
  되살리려면 git 이력에서 `logic/sim.js` 와 `screens/45-sim.html` 을 꺼내면 됩니다.

### 화면 ↔ 파일 대응

| 조각 파일 | 화면 | 켜지는 조건 |
| --- | --- | --- |
| `_shell-head.html` | `<head>`·헤더·`<main>` 여는 태그 | 항상 |
| `00-data-error.html` | 자료 못 불러왔을 때 | `dataError` |
| `01-home.html` | 첫 화면 (시·도 → 구 → 상권 검색) | `onHome` |
| `02-overlay.html` | 도우미 창·소개 안내창 | `botOpen` 등 |
| `03-settings.html` | 설정 (언어 · 테마) | `settingsOpen` |
| `05-hub.html` | ①② 메뉴의 허브 | `onHub` |
| `10-report.html` | ④ 리포트 (설문 → 지원사업) | `onReport` |
| `20-price.html` | ③ 통합시세 (왼쪽 세로 갈래·지표 · 오른쪽 가로 차트) | `onPrice` |
| `30-fine-intro.html` | ② 정밀분석 소개 | `onFineIntro` |
| `31-fine-compare.html` | ① 자치구 훑기 | `onFineCmp` |
| `32-map.html` | ② 지도 | `onMapScreen` |
| `33-fine-detail.html` | ② 정밀분석 (매출·수요·경쟁·비용·시장구조 탭) | `onFineDetail` |
| `40-zone-compare.html` | ① 지역비교 | `onZoneCmp` |
| `41-region.html` | ① 고른 지역의 업종 목록 | `onRegion` |
| `42-find.html` | ① 후보지 | `onFind` |
| `43-diagnosis.html` | ② 본전 계산 | `onDiag` |
| `45-sim.html` | ② 정밀비교 (담은 상권 종합순위) | `onSim` |
| `50-ai.html` | 도우미 전체 화면 | `onAi` |
| `51-soon.html` | 준비 중 | `onSoon` |
| `_shell-foot.html` | `</main>` 닫기·스크립트 | 항상 |

순서는 `scripts/build-html.mjs` 의 `ORDER` 가 정합니다.

---

## 5. 로직 모듈

`frontend/logic/*.js` 는 각각 `globalThis.MysbizonParts.<이름>` 에 객체를 하나 얹고,
`app-logic.js` 맨 아래(1351행)가 **프로토타입으로 합칩니다.**

```js
for (const name of ['i18n','theme','roman','util','design','rank','analysis',
                    'screens','chat','charts','carousel','market','views']) { ... }
```

이 결합기는 **메서드 이름이 겹치면 예외를 던집니다.** 새 메서드를 만들 때 이름이 안 겹치는지 보세요.
새 모듈을 추가하면 **위 배열과 `tests/ui-logic.test.js`·`tests/i18n.test.js` 의 `LOGIC_PARTS`,
그리고 `screens/_shell-head.html` 의 `<script>` 순서**에 같이 넣어야 합니다. 안 넣으면 테스트가 바로 잡습니다.

| 파일 | 맡은 일 |
| --- | --- |
| `const.js` | 여러 곳이 함께 쓰는 목록 |
| `i18n.js` | 다국어 — `t()` · `tn()`(조사) · `tr()`(문장 번역) · `trDeep()` · `trDom()` |
| `theme.js` | 테마 5종 · 라이트/다크/시스템 · 색 직접 고르기 |
| `roman.js` | 한글 → 로마자 (영어 화면의 상권 이름). 자음 동화 규칙 포함 |
| `util.js` | 값 다듬기 · 이름 바꾸기 · 언어별 숫자/금액 표기 |
| `design.js` | 카드·제목·숫자 스타일, '숫자 → 해석' |
| `rank.js` | **종합순위 엔진** — min-max 정규화 + 가중치, 상권별 색 슬롯 고정 |
| `analysis.js` | 순위·지도·정밀분석 섹션 계산 |
| `screens.js` | 화면별 값 묶음 (홈·지역비교·후보지) |
| `chat.js` | 도우미 (규칙 기반. 외부 생성형 AI 호출 없음) |
| `charts.js` | Chart.js 래퍼 |
| `carousel.js` | 가로 슬라이드 (scroll-snap + 드래그 + 휠). `peek:false` 좁은 칸용 · `arrows:true` 모바일 화살표 |
| `market.js` | **통합시세** — 7갈래 28지표 · 왼쪽 세로 목록(`mk.side`) |
| `views.js` | 자료가 있어야 만들 수 있는 화면 조립 |
| `app-logic.js` | 상태 · 생애주기 · `MENU` · `renderVals()` · 결합 |

### 템플릿 프레임워크 (DC)

`frontend/dc-runtime.js` — React 기반 벤더 코드입니다. **고치지 마세요.**

```html
<sc-if value="{{ onHome }}"> … </sc-if>
<sc-for list="{{ items }}" as="o"> {{ o.label }} </sc-for>
```

값은 전부 `renderVals()` 가 만들어 내려보냅니다. 이벤트도 `go:()=>this.setState(...)` 처럼 값으로 넘깁니다.

---

## 6. 다국어 (KR / EN / 中文)

두 겹으로 되어 있습니다.

1. **사전 키** — `locales/{ko,en,zh-CN}.json` 의 169개 키. `t('nav.zone')` 로 씁니다.
2. **문장 기억(@phrases)** — 한국어 원문 → 번역. 화면 조각에 그대로 적힌 한국어를 그린 뒤 훑어서 바꿉니다(`trDom`).
   숫자는 `{0}` 으로 정규화해서 찾습니다 — `"1,096곳"` 하나만 넣어 두면 `"2,431곳"` 도 번역됩니다.

**규칙**

- 새 한국어 문구를 넣으면 **세 언어 사전에 같이** 넣습니다. `npm test` 가 키 개수 불일치를 잡습니다.
- 컴포넌트에 한국어를 하드코딩하지 마세요.
- 중국어를 영어로 대신 채우지 마세요.
- 상권·구·동 이름은 고유명사라 번역 대상이 아닙니다. 영어에서는 `roman.js` 가 로마자로 바꿉니다(`성수역` → `Seongsu Stn.`).
  중국어에서는 **한글 그대로 남습니다** — 한자 대응표가 없습니다. 알려진 한계입니다.

---

## 7. 디자인 규칙

> **DATA-RICH · UI-LIGHT.** 기능과 데이터는 깊어도 되지만, 한 화면에 동시에 보이는 것은 적어야 합니다.
> 한 화면은 **질문 하나**에만 답합니다 — 제목 1 · 핵심 숫자 1~2 · 설명 1줄 · 차트 1 · 주 버튼 1.
> 나머지는 가로 슬라이드 · 탭 · 접기 · 상세보기로 **눌렀을 때만** 보여 줍니다(progressive disclosure).

- **카드는 정말 강조가 필요할 때만.** 카드 안에 카드를 넣지 않습니다. 기본 그룹은 여백으로 묶습니다.
- **그림자를 쓰지 않습니다.** 구분은 여백 → 글자 크기 → 구분선 → 테두리 순으로 풉니다.
- **민트는 화면당 1~2곳.** 카드 배경·숫자·태그·버튼·선택·차트에 동시에 쓰지 않습니다.
- **채워진 버튼은 화면당 하나.** 나머지는 글자 버튼입니다.
- **차트는 한 화면에 하나.** 여러 개면 가로 슬라이드로 넘깁니다. 차트 제목은 사용자의 질문으로 씁니다.
- **모바일은 데스크톱을 세로로 쌓은 게 아닙니다.** 데스크톱 세로 사이드바는 모바일에서 가로 탭이 됩니다.
- **긴 회색 설명 금지.** 기본 화면 설명은 한 줄. 출처·계산방법은 `ⓘ 한 줄 + 데이터 기준 보기` 로 접습니다.
- **기존 민트·화이트 색감을 유지합니다.** 전면 재설계 금지.
- 색은 `--color-*` 토큰으로만 씁니다. **hex 를 컴포넌트에 반복해 적지 마세요.**
- 비교 화면에서 **상권마다 색이 고정**입니다(`rank.js` 의 색 슬롯). 차트가 바뀌어도 같은 상권은 같은 색이어야 합니다.
  팔레트는 색맹 안전(Okabe-Ito) 기준입니다.
- **다크 모드를 CSS `filter` 로 만들지 마세요.** 토큰을 다시 정의하는 방식입니다(`theme.js`).
- 숫자만 잔뜩 늘어놓고 끝내지 마세요. 화면마다 **"그래서 뭐가 어떻다"** 는 한 줄이 있어야 합니다.

---

## 8. 보안 — 타협 없는 선 (자세한 건 `CLAUDE.md §2`)

- **API 키·비밀번호를 코드나 저장소에 절대 넣지 않습니다.** `.env`(커밋 금지) / 환경변수만.
- API 엔드포인트는 **origin-lock** 을 유지합니다(`api/_origin.js`). 운영에서는 `ALLOWED_ORIGIN` 이 없으면 서버가 아예 뜨지 않습니다.
- `.env` 내용 · 개인키 · 인증서 private key · DB 비밀번호 · JWT secret 은 **출력하지 않습니다.**
- 비밀정보가 한 번이라도 저장소에 올라갔다면 삭제로 끝내지 말고 **키 재발급(rotation)** 이 필요하다고 알립니다.

## 9. 요청 없이 하면 안 되는 것 (`CLAUDE.md §5`)

운영 DB 삭제 · 데이터 초기화 · 서버 초기화 · 방화벽 변경 · root 설정 변경 ·
패키지 전체 업그레이드 · DB migration 강제 실행 · production 데이터 수정 · 비밀키 출력.

그리고 **기존 기능을 임의로 지우지 마세요.** 지워야 할 것 같으면 먼저 물어보세요.

---

## 10. 지금 알려진 미완 · 함정

작업 전에 이걸 모르면 "고장 났다"고 오해하기 쉬운 것들입니다.

| | 내용 |
| --- | --- |
| 🟡 | **`api/market.js` 가 서버에서 라우팅되지 않습니다.** `server/app.js` 의 `PUBLIC_APIS` 에 `'report','config','support'` 만 있어서 `/api/market` 은 404 입니다. 지금은 프론트가 이 주소를 부르지 않아(로컬 상태로 '준비 중'만 표시) 문제가 드러나지 않습니다. **키를 넣어 통합시세를 살릴 때 `PUBLIC_APIS` 에 `'market'` 추가 + `logic/market.js` 에 fetch 연결이 같이 필요합니다.** |
| 🟡 | **통합시세 28지표 중 6개만 실제로 그려집니다.** 나머지 22개(환율·금리·물가·농축수산물·에너지)는 `ECOS_KEY` / `KAMIS_KEY`+`KAMIS_ID` / `OPINET_KEY` 가 없어 '데이터 준비 중'입니다. **목록에서 지우지 마세요.** 통계표 코드는 문서에서 찾은 값이라 실제 응답으로 확인이 안 됐습니다. |
| 🟡 | **상권 단위 임대료가 아직 없습니다.** 지금은 한국부동산원 임대동향조사(서울 63개 상권 + 서울 평균)를 씁니다. 이름이 정확히 맞는 상권만 그 값을 쓰고 나머지는 '서울 평균'으로 내려갑니다. `ZONE_RENT_SERVICE` 오퍼레이션명 확인이 필요합니다. |
| 🟡 | **예측 체계가 둘입니다.** `signals.json`(엄격한 홀드아웃)과 `sales_forecast.json`(사실상 in-sample 선택). 후자는 기준이 관대합니다. 통합 여부는 아직 판단 전입니다. |
| 🟡 | `.github/workflows/refresh-dashboard.yml` 의 **수집 단계가 전부 `continue-on-error: true`** 라, 전부 실패해도 워크플로가 초록색으로 끝납니다. |
| 🟡 | `frontend/region_chungnam.json` 이 없습니다(충북은 있음). 충남 화면은 인구 지표가 빠진 채 뜹니다 — 정직하게 degrade 하지만 충북과 보이는 정보가 다릅니다. |
| 🟢 | 콘솔에 `{{ s.d }}` 같은 SVG 속성 오류가 뜹니다. DC 프레임워크가 하이드레이션 전 자리표시자를 그려서 생기는 것으로 **화면·기능에 영향 없습니다.** 고치지 마세요. |
| 🟢 | 정밀비교의 예상 월매출 기본값은 상권 자료의 **점포당** 월매출을 그대로 넣은 값입니다. 상권에 따라 커 보일 수 있습니다(예: 5.2억). 계산은 맞고, 화면에 `상권 자료에서 넣어 둔 값이에요` 라고 표시하며 사장님이 고칠 수 있습니다. |

나머지 미확인 항목·사장님이 직접 해야 하는 것(공공데이터 활용신청 등)은 **`CLAUDE.md §25 살아있는 체크리스트`** 에 전부 있습니다.
새 위험을 발견하면 거기에 추가하세요.

---

## 11. 고치기 전에 스스로 물어볼 것

1. **목적** — 무엇을 바꾸는가?
2. **영향범위** — 어떤 파일 / 데이터 / API 에 영향?
3. **위험** — 무엇이 깨질 수 있는가? (🟢 낮음 / 🟡 주의 / 🔴 위험)
4. **테스트** — `npm test` 27개가 그대로 통과하는가?
5. **롤백** — 실패하면 어떻게 되돌리는가?

작업이 끝나면 반드시:

```bash
npm run build:html && npm test && npm run check:data
```
