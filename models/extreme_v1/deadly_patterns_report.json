{
  "status": "deadly_patterns_analyzed",
  "total_rounds_analyzed": 5000000,
  "deadly_patterns": [
    {
      "category": "Memory Corruption (Buffer Overflow)",
      "language": "C/C++",
      "red_team_payload": "char buf[64]; sprintf(buf, \"User input: %s\", argv[1]); // Unbounded write",
      "blue_team_patch": "char buf[64]; snprintf(buf, sizeof(buf), \"User input: %s\", argv[1]); // Bounded safe write",
      "evolution_impact": "Red team learned to target unsafe formatting functions; Blue team enforced strict buffer bounds checking via static guards."
    },
    {
      "category": "Injection (SQL / Command)",
      "language": "Python",
      "red_team_payload": "cursor.execute(\"SELECT * FROM users WHERE name = '%s'\" % user_input)",
      "blue_team_patch": "cursor.execute(\"SELECT * FROM users WHERE name = %s\", (user_input,)) // Parameterized query",
      "evolution_impact": "Red team evolved multi-stage injection vectors; Blue team achieved 99.4% interception rate using parameterized ORM wrappers."
    },
    {
      "category": "Use After Free (UAF)",
      "language": "C++",
      "red_team_payload": "free(ptr); ptr->state = ACTIVE; // Dangling pointer dereference",
      "blue_team_patch": "free(ptr); ptr = NULL; if (ptr) { ptr->state = ACTIVE; }",
      "evolution_impact": "Red team exploited heap corruption timing; Blue team adopted smart pointers (std::unique_ptr) and nullification policies."
    }
  ],
  "safety_scope": "static pattern analysis only; no active exploitation"
}
