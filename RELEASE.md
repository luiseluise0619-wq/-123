# RELEASE — 출시 전 점검표

> **이 문서는 "출시해도 되는가"를 한 장으로 답한다.**
> 항목마다 **어떻게 확인했는지**와 **결과**를 적었다. 확인 안 한 것은 "확인 못 함"이라고 적는다 —
> 넘어간 것을 통과로 적지 않는다(§1 데이터 정직성).
>
> 표기: ✅ 통과 · ⬜ 사장님이 해야 함(코드로 못 함) · ⚠️ 확인 필요 · ❌ 막는 것
> 위험도: 🟢 낮음 · 🟡 주의 · 🔴 위험
>
> 마지막 점검: **2026-09-08** · Node 22 · Chromium(헤드리스) · 로컬 서버

---

## 0. 지금 출시할 수 있나 — 한 줄 답

**기능·안전은 준비됐다. 남은 것은 사람이 해야 하는 것 세 가지다.**

| 막는 것 | 왜 | 누가 |
| --- | --- | --- |
| ⬜ 배포 대상 확정 + 실제 배포 1회 | 아래 §2 참고 — Render(Node) 와 Vercel(정적) 은 되는 것이 다르다 | 사장님 |
| ⬜ 도메인 + HTTPS + `ALLOWED_ORIGIN` | 없으면 프록시가 아무 사이트에서나 열린다 | 사장님 |
| ⬜ 카카오 JS 키 도메인 등록(지도를 켤 때만) | 콘솔 설정이라 코드로 못 막는다 | 사장님 |

그 외 **키가 없어도 서울 상권 1,564곳 × 업종 62가지 분석은 전부 동작한다.**
무엇을 넣으면 무엇이 살아나는지는 `SETUP.md`.

---

## 1. 코드·품질

| # | 항목 | 확인 방법 | 결과 |
| --- | --- | --- | --- |
| 1.1 | 자동 시험 | `npm test` | ✅ 38개 전부 통과 |
| 1.2 | `index.html` 이 조각과 일치 | `npm run check:html` | ✅ 일치 |
| 1.3 | 화면이 쓰는 자료 검증 | `npm run check:data` | ✅ 통과 |
| 1.4 | JS 문법 | 커밋된 `.js`·`.mjs` 전부 `node --check` | ✅ 오류 0 |
| 1.5 | Python 수집기 문법 | `py_compile` 77개 | ✅ 오류 0 |
| 1.6 | 외부 npm 의존성 | `package.json` | ✅ **0개** — 공급망 위험이 구조적으로 없다 |
| 1.7 | 화면 값에 NaN·undefined | `renderVals()` 전수 훑기 840조합 + 무작위 퍼징 1,350회 | ✅ 이상 0 |
| 1.10 | 무결성 목록 | `npm run check:sums` | ✅ **고침** — `SHA256SUMS.json` 이 52개 중 **21개가 안 맞았다**(낡아서). 다시 만들었고, `npm run sums`/`check:sums` 를 뒀다 — **배포 직전에 한 번 돌릴 것** |
| 1.11 | 옛 점검 기록 | `AUDIT.md` · `QA-RESULTS.md` · `verification.json` | ⚠️ **2026-09-05 시점 기록**이다(시험 13개 시절). 지금 상태는 **이 문서(RELEASE.md)** 가 맞다 |
| 1.9 | 정적 서버 안전장치 | `server/static.js` | ✅ realpath 로 심볼릭 링크 탈출 차단 · 루트 밖 차단 · 확장자 허용 목록 · 10MB 상한 · 점(.)으로 시작하는 경로 차단 |
| 1.8 | 무작위 클릭 훑기 | 3언어 × 320·390·768·1280 × 여러 씨앗 | ✅ 오류 0 · 이상 글자 0 |

## 2. 배포 — 🔴 먼저 정할 것

**두 갈래가 저장소에 같이 들어 있다. 하나를 골라야 한다.**

| | Render / Cafe24 VPS (Node 서버) | Vercel (정적 + 서버리스) |
| --- | --- | --- |
| 설정 | `render.yaml` · `deploy/mysbizon.service` · `deploy/nginx.conf.example` | `frontend/vercel.json` |
| 정적 파일 | `server/static.js` 가 서빙 | Vercel 이 서빙 |
| `/api/*` | `server/app.js` 허용 목록(`report`·`config`·`support`) | 루트 `api/*.js` 가 함수로 뜬다 |
| 보안 헤더·CSP | ✅ `server/security.js` 가 붙인다 | ⚠️ **`vercel.json` 에 `headers` 를 넣어야 같아진다** |
| 요청 제한(rate limit) | ✅ 서버 안에 있다 | ❌ 없다 — 서버리스는 메모리를 공유하지 않는다 |

| # | 항목 | 확인 방법 | 결과 |
| --- | --- | --- | --- |
| 2.1 | Vercel 홈 화면 경로 | `frontend/vercel.json` 의 rewrite 확인 | ✅ **고침** — `/zone.html`(지운 파일)로 보내고 있었다. 지우고 `index.html` 이 뜨게 했다 |
| 2.2 | Vercel 보안 헤더 | `vercel.json` `headers` | ✅ **넣음** — Node 서버와 같은 CSP·X-Frame-Options 등 |
| 2.3 | 실제 배포 1회 | — | ⬜ **아직 안 함.** 이 작업 환경에서는 배포할 수 없다 |
| 2.4 | `NODE_ENV=production` 이면 `ALLOWED_ORIGIN` 필수 | `server.js` 첫 부분 | ✅ 없으면 **뜨지 않는다**(조용히 열리지 않는다) |
| 2.5 | health check | `GET /healthz` | ✅ `{"ok":true}` |
| 2.6 | 종료 신호 처리 | `server.js` SIGTERM/SIGINT | ✅ 15초 안에 정리 후 종료 |
| 2.7 | systemd 강화 | `deploy/mysbizon.service` | ✅ 전용 계정 · `ProtectSystem=strict` · `NoNewPrivileges` · 메모리 512M |
| 2.8 | nginx 예시 | `deploy/nginx.conf.example` | ✅ HTTPS 강제 · 요청/연결 제한 · **`access_log off`**(주소·이메일이 로그에 안 남는다) |
| 2.10 | 배포 용량 | `frontend/` 6.5MB — 그중 화면이 실제로 받는 건 `data/v3` 10개 + `zone_rent.json` | ✅ 첫 로드 **630KB(gzip)**. 나머지 4MB 는 '아직 화면에 안 붙인 수집 자료'라 일부러 둔다(CLAUDE.md §25) — 아무도 안 받으므로 느려지지 않는다 |
| 2.9 | Node 버전 | `package.json` engines `>=22 <25` / render `NODE_VERSION=24` | ✅ 맞음 |

## 3. 보안 (§2)

| # | 항목 | 확인 방법 | 결과 |
| --- | --- | --- | --- |
| 3.1 | 저장소에 비밀키 | 커밋된 파일 전수 스캔(키 모양·긴 랜덤 문자열) | ✅ **없다.** 워크플로는 전부 `${{ secrets.* }}` 참조 |
| 3.2 | 코드에 박힌 시크릿 기본값 | 같은 스캔 | ✅ **고침** — `backend/app/core/config.py` 의 `SECRET_KEY` 에 값이 박혀 있었다(§2 위반). 환경변수로 바꿨다 |
| 3.3 | 카카오 JS 키가 소스에 | 저장소 전체 grep | ✅ **없다.** 그 파일(`frontend/building.html`)은 이미 지워졌다 |
| 3.4 | `.env` 커밋 방지 | `.gitignore` | ✅ `.env`·`.env.*` 제외, `.env.example` 만 남김 |
| 3.5 | origin-lock | `Origin` 없이 `POST /api/report` | ✅ 403 |
| 3.6 | API 허용 목록 | `/api/{report,config,support}` 외 호출 | ✅ 그 밖은 전부 404(`market`·`chat`·`lead` 포함) |
| 3.7 | 경로 탈출 | `..`·`%2e%2e`·`.git`·`.env`·`server/` 등 11가지 | ✅ 전부 404 |
| 3.8 | 보안 헤더 | 응답 헤더 확인 | ✅ CSP `default-src 'self'` · `X-Frame-Options: DENY` · `nosniff` · Referrer-Policy · Permissions-Policy |
| 3.9 | 요청 제한 | `server/app.js` | ✅ IP당 60회/분 · 리포트 3회/시간 · 전체 동시 20건 |
| 3.10 | 오류 메시지에 내부 정보 | `api/_err.js redact()` | ✅ 키 모양을 가리고, 사용자에게는 detail 을 안 준다 |
| 3.11 | 로그에 개인정보 | 서버·API 의 `console.*` 전수 | ✅ 이메일·IP 를 찍는 곳 없음 |
| 3.12 | 외부 스크립트 | `frontend/vendor` | ✅ React·Chart.js 를 저장소에 두고 CSP 가 `self` 만 허용 |
| 3.13 | 브라우저 저장값 검증 | 깨진 `localStorage` 8가지 | ✅ 전부 정상 폴백, 오류 0 |
| 3.15 | 큰 본문 거부 | 2MB POST | ✅ 413 |
| 3.16 | 깨진 JSON / 잘못된 형식 | `{broken` · `text/plain` | ✅ 400 · 415 |
| 3.17 | 요청 제한이 실제로 걸리나 | 리포트 5연타 | ✅ 429 + `Retry-After` |
| 3.18 | HEAD 요청 | 감시 도구가 쓴다 | ✅ 200 |
| 3.14 | 폰트 CDN | `_shell-head.html` | ⚠️ Pretendard 를 jsdelivr 에서 받는다. 막혀도 시스템 폰트로 동작하지만 글꼴이 달라진다(이 환경에서 파일을 못 받아 옮기지 못함) |

## 4. 데이터 정직성 (§1)

| # | 항목 | 확인 방법 | 결과 |
| --- | --- | --- | --- |
| 4.1 | 자료 파일끼리 참조 | 상권 1,564곳 × 자치구·경계·유동인구·상권변화·닮은상권 대조 | ✅ 어긋남 0 |
| 4.2 | 없는 값을 지어내지 않는가 | 자료 전부 차단 · 일부만 차단 | ✅ 안내만 뜨고 숫자를 만들지 않는다 |
| 4.3 | **망가진 자료**(반쪽 파일) | 잘린 JSON·빈 객체·배열·글자·항목 누락 × 12개 파일 | ✅ **고침** — 개발자 오류가 화면에 뜨던 것을 안내 화면으로 |
| 4.4 | 자료에 없는 자치구 | 가구지출 20/25곳 | ✅ **왜 빠졌는지 이름과 함께** 화면에 적는다 |
| 4.5 | 추정값 표기 | 환산임대료·회수기간·1인당 소비 | ✅ '추정'·계산식·한계를 화면에 적는다 |
| 4.6 | 자격 판정 안 함(§17) | 지원사업 절 | ✅ '조건에 해당할 수 있는 공고' + 원문 링크. 자격을 판정하지 않는다고 명시 |
| 4.7 | 기준 시점 표기 | 화면 머리·리포트 | ✅ `2026년 1분기 자료 기준` |

## 5. 화면 · 사용성

| # | 항목 | 확인 방법 | 결과 |
| --- | --- | --- | --- |
| 5.1 | 3언어 번역 누락 | 화면 값 + 마크업 + 도우미 + 인쇄본 전수 대조 | ✅ 남은 한국어 **0**(언어 선택의 '한국어'만 일부러 남김) |
| 5.2 | 대비(WCAG AA) | 프리셋 5 × 밝게·어둡게 + 직접 고른 색 10가지 | ✅ 미달 0 |
| 5.3 | 가로 넘침 | 3언어 × 320·390·768·1280 | ✅ 0 |
| 5.4 | 말줄임으로 잘린 글자 | 무작위 클릭 중 `scrollWidth>clientWidth` | ✅ 0 |
| 5.5 | 키보드만으로 사용 | Tab 40회 · Esc · Enter | ✅ 초점 테두리 전부 보임 · 설정·도우미 Esc 로 닫힘 |
| 5.6 | 뒤로 가기 | 화면 3번 이동 후 3번 복귀 | ✅ 처음 화면으로 정확히 복귀 |
| 5.7 | 새로고침에 설문 유지 | `sessionStorage` | ✅ 유지 |
| 5.8 | 느린 응답 중 마구 클릭 | 1.2초 지연 + 난타 | ✅ 오류 0 |
| 5.9 | 오래 쓰면 무거워지나 | 화면 200번 · 언어 30번 전환 | ✅ 노드 97→111 · heap 8→24MB · 오류 0 |
| 5.10 | 성능 | 첫 화면 | ✅ FCP 210~280ms · 첫 로드 630KB(ko)/667KB(en) · 실패 응답 0 |
| 5.12 | 접근성 뼈대 | 5개 화면 × 랜드마크·제목 단계·이름 없는 버튼·라벨 없는 입력칸 | ✅ 문제 0 (`main` 1개 · `h1` 1개 · 건너뛰기 링크 있음) |
| 5.13 | `<html lang>` 이 언어를 따르는가 | ko·en·zh 로 열어 확인 | ✅ **고침** — 늘 `ko` 였다. 스크린리더가 영어를 한국어 발음으로 읽는다 |
| 5.14 | 전송 압축·캐시 | gzip · ETag | ✅ index.html 145KB → **27KB** · 재요청 304 |
| 5.11 | 실제 폰 | — | ⬜ **확인 못 함.** 헤드리스 브라우저로만 봤다 — 손가락 조작·스크롤 감각은 사람이 봐야 한다 |

## 6. 결과물(리포트·CSV·메일)

| # | 항목 | 확인 방법 | 결과 |
| --- | --- | --- | --- |
| 6.1 | PDF 인쇄본 | 값 있음/없음 · A4 생성 | ✅ 414KB 정상 · 오류 0 |
| 6.2 | 인쇄본이 화면 언어를 따르는가 | en·zh 로 미리보기 | ✅ 남는 한국어는 공고 원문 금액·상권 이름뿐(옮기지 않는 게 맞다) |
| 6.3 | 어두운 화면에서 인쇄 | 두 PDF 비교 | ✅ 바이트까지 동일 — 검은 종이가 안 나온다 |
| 6.4 | CSV | 줄 수 · BOM · 수식 주입 · 파일 이름 | ✅ 22줄 · BOM O · 주입 0 · ASCII 이름 |
| 6.6 | 메일이 화면 언어를 따르는가 | `api/report.js` | ✅ **고침** — 제목을 서버가 한국어로 덮어쓰고 있었다. 화면이 보낸 제목을 쓴다(길이 제한·escape 그대로). **면책 문구는 서버가 고정**한다 — 화면이 약하게 바꿀 수 있으면 안 된다(§17) |
| 6.7 | 메일 본문에 HTML 주입 | `esc()` + 길이 제한 + 항목 30개 제한 | ✅ 전부 escape · 동의 없으면 400 |
| 6.8 | 실제 메일 발송(Brevo) | — | ⬜ **확인 못 함.** 키가 없어 실제로 보내 보지 못했다. 켤 때 한 통 보내 확인할 것 |
| 6.5 | 메일 발송 | 기본값 | ✅ **꺼져 있다**(`REPORT_EMAIL_ENABLED=false`). 켤 때는 §7 확인 |

## 7. 법적 · 운영 (§17 — 법률 판단은 확인 필요 사항으로만 적는다)

| # | 항목 | 결과 |
| --- | --- | --- |
| 7.1 | 개인정보 안내 페이지 | ✅ `/privacy.html` 존재 · 화면에서 링크 |
| 7.2 | 운영 주체·연락처·보관 기간 | ⬜ **비어 있다.** 개인정보 안내에 "발송 기능을 열기 전에 게시해야 한다"고만 적혀 있다 → 회사 이름·연락처·보관 기간을 채울 것 |
| 7.3 | 이메일 수집 동의 | ✅ 체크박스 + 수탁사(Brevo) 명시 |
| 7.4 | 공공데이터 출처 표기 | ✅ 화면·인쇄본 맨 아래 |
| 7.5 | 오픈소스 고지 | ✅ `THIRD-PARTY.md` · `licenses/` · 화면 소개창 |
| 7.6 | 면책 | ✅ '투자 권유나 수익 보장이 아닙니다' |
| 7.7 | 공공데이터 재배포 조건 | ⚠️ **확인 필요 사항.** 수집한 JSON 을 그대로 서비스에 싣고 있다. 각 기관 이용조건에서 재배포 가능 범위를 사람이 확인할 것 |

## 8. 운영 준비

| # | 항목 | 결과 |
| --- | --- | --- |
| 8.1 | 자동 수집(GitHub Actions) | ✅ 주 1회 + 월 1회 예약 |
| 8.2 | 수집 실패를 알아채는가 | ✅ 마지막에 `report_freshness.py` 가 **파일을 직접 읽어** 표로 요약하고, 수치가 전부 죽으면 워크플로를 빨간불로 만든다(부분 실패로는 안 막는다 — 매주 빨간불이면 아무도 안 본다) |
| 8.2b | 지금 자료가 신선한가 | ✅ `python report_freshness.py` — **수치 17/17 정상**(2026-08-20~31), 보존 4개 정상, 승인 대기 2개 |
| 8.2c | 자료 값이 말이 되는가 | ✅ `python check_data.py` — **정상 12 · 경고 0 · 실패 0**(분포합·객단가·폐업률·소비구성·생존개월·백분위) |
| 8.3 | 백업 | ⬜ 지금은 DB 가 없어 **저장소가 곧 백업**이다. VPS 로 옮기고 DB 를 쓰기 시작하면 `pg_dump` + cron + **복구 테스트 1회** |
| 8.4 | 모니터링 | ⬜ 없음. `/healthz` 를 외부에서 1분마다 찌르는 무료 감시(UptimeRobot 등)만 걸어도 충분한 단계다 |
| 8.5 | 롤백 | ✅ 배포는 git 기반이라 이전 커밋으로 되돌리면 된다 |
| 8.6 | 검색엔진 | ✅ **넣음** — `robots.txt`. 출시 전까지 숨기려면 `Disallow: /` 로 바꿀 것 |

---

## 8-1. 배포 직전에 돌릴 명령 (순서대로)

```bash
npm test          # 38개 — 하나라도 실패하면 배포하지 않는다
npm run check:html   # index.html 이 조각과 맞는가
npm run check:data   # 화면이 읽는 자료가 온전한가
cd backend && python report_freshness.py && python check_data.py && cd ..
npm run sums      # SHA256SUMS.json 을 이 배포본으로 다시 만든다 (마지막)
```

## 9. 사장님이 해야 하는 것 (코드로 못 하는 것) — 순서대로

1. **배포 대상 하나 정하기** (§2 표) → 그 경로로 **실제 한 번 배포**
2. **도메인 연결 + HTTPS** → `ALLOWED_ORIGIN` 에 그 도메인 넣기 (없으면 서버가 안 뜬다)
3. **개인정보 안내에 운영 주체·연락처·보관 기간 채우기** (7.2)
4. `/healthz` 외부 감시 걸기 (8.4)
5. (선택) 키를 넣어 기능 늘리기 — `SETUP.md` 순서대로: 지원사업 → 통합시세 → 지도 → 메일
6. (지도를 켤 때) 카카오 developers 에서 **플랫폼 > 사이트 도메인 등록** — 안 하면 남이 우리 호출 한도를 쓴다
7. **공공데이터 재배포 조건 확인** (7.7)
